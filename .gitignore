# rebuilt by experiments/corpora/*.py -- 6.5 GB, never committed
experiments/corpora/*/corpus.parquet
experiments/corpora/*/*.npy
*.log
__pycache__/
target/

# pre-push hygiene tool: names the identifiers it looks for, so it
# must not be published alongside an anonymised artifact
check_anonymity.sh
